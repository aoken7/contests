#include<bits/stdc++.h>
#define all(a) a.begin(), a.end()
#define put(i) cout<<i<<endl
#define rep(i,s,n) for(long long i=s;i<(long long)(n);i++)
using namespace std;
using ll = long long;

int main(){
    ll n, m;
    cin >> n >> m;
    vector<vector<ll>> b(n,vector<ll>(m));
    rep(i,0,n){
        rep(j,0,m){
            cin >> b[i][j];
        }
    }


    rep(i,0,n){
        rep(j,0,m-1){
            if (b[i][j] != b[i][j+1] - 1 or b[i][j] % 7 == 0 and b[i][j+1] % 7 == 1){
                put("No");
                return 0;
            }
        }
    }

    rep(i,0,n-1){
        if (b[i][0] != b[i+1][0] - 7){
            put("No");
            return 0;
        }
    }

    
    put("Yes");
}
