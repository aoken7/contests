#include <bits/stdc++.h>
#define all(a) a.begin(), a.end()
#define put(i) cout << i << endl
#define rep(i, s, n) for (long long i = s; i < (long long)(n); i++)
using namespace std;
using ll = long long;

const double PI = acos(-1);

ll t;
ll l, x, y;

ll solve() {
    ll q;
    cin >> q;
    rep(i, 0, q) {
        ll e;
        cin >> e;
        ll _y = -l / 2 * sin(PI * (e / t * 360));
        ll _z = l / 2 * l / 2 * cos(PI * (e / t * 360));
        
    }
}

int main() {
    cin >> t;
    cin >> l >> x >> y;
}
